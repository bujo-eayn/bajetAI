# Supabase Configuration
# Get these values from your Supabase project dashboard (Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Hugging Face API (Phase 5: AI Summarization)
# Get your API key from https://huggingface.co/settings/tokens
# Free tier: 24,000 requests/month (~800/day)
HUGGING_FACE_API_KEY=your-huggingface-api-key-here

# Optional: Hugging Face API timeout in milliseconds (default: 30000, max: 60000)
# Increase for very large documents
HUGGING_FACE_TIMEOUT_MS=30000

# Optional: Hugging Face daily rate limit (default: 800)
# Free tier allows ~24,000 requests/month (~800/day)
# When limit is exceeded, system automatically uses fallback summarization
HUGGING_FACE_DAILY_LIMIT=800

# Optional: UTC hour to reset rate limit counter (default: 0 = midnight UTC)
# Set to your timezone offset for convenience (e.g., 8 for UTC+8)
HUGGING_FACE_RESET_HOUR=0

# OpenAI API (Optional: Backup Fallback for HuggingFace)
# Get your API key from https://platform.openai.com/api-keys
# Used automatically when HuggingFace is unavailable
# Cost: ~$0.002 per 1000 tokens (very affordable)
# OPENAI_API_KEY=your-openai-api-key-here

# Inngest Configuration (Phase 4: PDF Extraction)
# Get these from https://www.inngest.com/dashboard
INNGEST_EVENT_KEY=your-inngest-event-key-here
INNGEST_SIGNING_KEY=your-inngest-signing-key-here

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Optional: Analytics
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# Ollama API
OLLAMA_API_KEY=your_api_key